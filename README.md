# 做菜小程序后台管理系统前端 

基于 Vue 3 + Element Plus 的后台管理系统前端项目。

## 技术栈

- **Vue 3**: 渐进式 JavaScript 框架
- **Vite**: 现代化的前端构建工具
- **Element Plus**: Vue 3 组件库
- **Vue Router**: 官方路由管理器
- **Pinia**: 状态管理库
- **Axios**: HTTP 客户端
- **Sass**: CSS 预处理器

## 功能特性

### 🔐 认证系统
- 管理员登录/登出
- JWT Token 认证
- 路由守卫保护
- 自动 Token 刷新

### 📊 仪表盘
- 数据统计展示
- 快捷操作入口
- 系统概览信息

### 🗂️ 分类管理
- 分类列表查看
- 新增/编辑/删除分类
- 分类状态管理
- 搜索和分页

### 🍽️ 菜品管理
- 菜品列表管理
- 菜品信息编辑
- 上架/下架操作
- 按分类筛选
- 图片预览功能

### 🖼️ Banner管理
- 轮播图管理
- 链接类型配置
- 排序和状态控制
- 图片预览

### 👤 个人信息
- 管理员信息查看
- 个人资料修改
- 登录记录查看

## 项目结构

```
src/
├── api/              # API 接口
│   ├── auth.js       # 认证相关
│   ├── category.js   # 分类管理
│   ├── dish.js       # 菜品管理
│   └── banner.js     # Banner管理
├── components/       # 公共组件
├── layout/           # 布局组件
│   └── index.vue     # 主布局
├── router/           # 路由配置
│   └── index.js      # 路由定义
├── stores/           # 状态管理
│   └── auth.js       # 认证状态
├── styles/           # 样式文件
│   └── index.scss    # 全局样式
├── utils/            # 工具函数
│   └── request.js    # HTTP 请求封装
├── views/            # 页面组件
│   ├── Login.vue     # 登录页
│   ├── Dashboard.vue # 仪表盘
│   ├── Category.vue  # 分类管理
│   ├── Dish.vue      # 菜品管理
│   ├── Banner.vue    # Banner管理
│   └── Profile.vue   # 个人信息
├── App.vue           # 根组件
└── main.js           # 入口文件
```

## 快速开始

### 1. 安装依赖

```bash
npm install
```

### 2. 启动开发服务器

```bash
npm run dev
```

访问 http://localhost:3000

### 3. 构建生产版本

```bash
npm run build
```

### 4. 预览生产版本

```bash
npm run preview
```

## 配置说明

### 环境配置

项目使用 Vite 的代理功能转发 API 请求到后端服务器：

```javascript
// vite.config.js
server: {
  port: 3000,
  proxy: {
    '/admin': {
      target: 'http://localhost:8080',
      changeOrigin: true
    }
  }
}
```

### API 基础配置

```javascript
// src/utils/request.js
const request = axios.create({
  baseURL: '/admin',
  timeout: 10000
})
```

## 默认账号

- **用户名**: admin
- **密码**: 123456

## 主要功能说明

### 认证流程

1. 用户在登录页输入用户名和密码
2. 前端调用登录 API 获取 JWT Token
3. Token 存储在 localStorage 中
4. 后续请求自动在请求头中携带 Token
5. Token 过期时自动跳转到登录页

### 数据管理

- **分类管理**: 支持分类的增删改查，状态控制
- **菜品管理**: 完整的菜品信息管理，包括图片、分类、难度等
- **Banner管理**: 轮播图管理，支持不同链接类型

### 响应式设计

- 使用 Element Plus 的栅格系统
- 适配不同屏幕尺寸
- 移动端友好的交互体验

## 开发规范

### 代码风格

- 使用 Vue 3 Composition API
- 组件采用 `<script setup>` 语法
- 使用 TypeScript 类型注解（可选）

### 命名规范

- 组件文件使用 PascalCase
- 路由名称使用 PascalCase
- API 函数使用 camelCase
- 常量使用 UPPER_SNAKE_CASE

### 目录结构

- 按功能模块组织代码
- 公共组件放在 components 目录
- 页面组件放在 views 目录
- 工具函数放在 utils 目录

## 部署说明

### Nginx 配置示例

```nginx
server {
    listen 80;
    server_name your-admin-domain.com;
    root /path/to/dist;
    index index.html;

    # 前端路由
    location / {
        try_files $uri $uri/ /index.html;
    }

    # API 代理
    location /admin/ {
        proxy_pass http://localhost:8080;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }
}
```

## 常见问题

### 1. 跨域问题

开发环境通过 Vite 代理解决，生产环境需要配置 Nginx 代理。

### 2. Token 过期

系统会自动检测 Token 过期并跳转到登录页，无需手动处理。

### 3. 图片显示问题

确保图片 URL 可访问，建议使用 CDN 或对象存储服务。

## 更新日志

### v1.0.0 (2024-01-27)

- ✨ 初始版本发布
- 🔐 完整的认证系统
- 📊 仪表盘功能
- 🗂️ 分类管理
- 🍽️ 菜品管理
- 🖼️ Banner管理
- 👤 个人信息管理

## 贡献指南

1. Fork 本仓库
2. 创建特性分支 (`git checkout -b feature/AmazingFeature`)
3. 提交更改 (`git commit -m 'Add some AmazingFeature'`)
4. 推送到分支 (`git push origin feature/AmazingFeature`)
5. 打开 Pull Request

## 许可证

本项目采用 MIT 许可证 - 查看 [LICENSE](LICENSE) 文件了解详情。